{
  "name": "ragdoll-test",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "npx playwright test --ignore-snapshots",
    "test:e2e": "npx playwright test tests/e2e --ignore-snapshots",
    "test:visual": "npx playwright test tests/visual",
    "test:visual:update": "npx playwright test tests/visual --update-snapshots"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.4",
    "@dimforge/rapier2d-compat": "^0.14.0",
    "@rive-app/canvas": "^2.37.0",
    "gsap": "^3.12.7"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.59.1",
    "lighthouse": "^13.1.0",
    "playwright-lighthouse": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
